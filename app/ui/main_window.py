"""PR003 – Anpassung für automatische WeekView."""

# Die bestehende MainWindow-Datei bleibt weitgehend unverändert.
# Ergänze nach der erfolgreichen KW-Auswahl:

# from app.services.shipment_service import get_week_dataframe
# from app.ui.week_view import WeekView
#
# def open_selected_week(self, week):
#     df = get_week_dataframe(week)
#     self.week_view = WeekView(df)
#     self.week_view.show()
